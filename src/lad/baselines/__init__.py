"""Vendored baseline implementations (CRAFT and InvertibleCE).

Both subpackages preserve their original author attribution and source links. See each
subpackage's ``ATTRIBUTION.md`` and module-level docstrings.
"""
